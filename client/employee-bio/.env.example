# Base URL for the Backend API
# For production (e.g. VPS), configure this to your subdomain's API proxy path:
# VITE_API_URL=https://bawaslu.naldera.tech/api
# For local development, it fallbacks to http://localhost:3030 if not defined
VITE_API_URL=http://localhost:3030
